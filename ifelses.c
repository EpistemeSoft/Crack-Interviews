#include<stdio.h>

main()
{
   int a, b;

   printf("Enter the values for A and B\n");
   scanf("%d %d", &a, &b);

   if( a > 0 )
   if( b > 0)
     printf(" B is greater than 0\n");
   else
     printf(" A is less than 0\n");
    
}

 
      
